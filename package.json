{
  "name": "scanner-service",
  "version": "1.0.0",
  "description": "REST Endpoint to invoke scanimage on linux machines (docker), process the scans with graphicsmagick and the scans as PDF file.",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "tsc && node lib/index.js",
    "debug": "node --inspect-brk index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^4.5.4",
    "ws": "^8.4.0"
  },
  "devDependencies": {
    "@types/ws": "^8.2.2"
  }
}
